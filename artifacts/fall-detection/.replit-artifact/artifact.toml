kind = "web"
previewPath = "/"
title = "Fall Detection Alert System"
version = "1.0.0"
id = "artifacts/fall-detection"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21721

[services.development]
run = "pnpm --filter @workspace/fall-detection run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fall-detection", "run", "build" ]
publicDir = "artifacts/fall-detection/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21721"
BASE_PATH = "/"
